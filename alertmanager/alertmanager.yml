global: {}

route:
  receiver: decision-agent
  group_by: ["alertname"]
  group_wait: 5s
  group_interval: 30s
  repeat_interval: 2m

receivers:
  - name: decision-agent
    webhook_configs:
      - url: "http://agent:8081/alertmanager"
        send_resolved: true
