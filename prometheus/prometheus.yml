global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: "metrics"
    static_configs:
      - targets: ["metrics:8000"]

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - alertmanager:9093
